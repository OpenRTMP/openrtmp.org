# SonarCloud automatic analysis settings.
#
# The German pages under de/ mirror the English pages line for line (same
# markup, same code samples), so they are excluded from copy-paste detection
# only. They are still analysed for bugs, vulnerabilities and code smells.
sonar.cpd.exclusions=de/**
